<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd" >
<mapper namespace="com.mallfe.item.mapper.UserMapper">

    <select id="selectBill" resultType="com.mallfe.item.pojo.Gj">
        select id,lsh,lrid,username,truename,status,
               case status when 0 then '新增' when 1 then '已提交' when 9 then '已作废' end as statusInfo,
               lrsj,cksj,lx, case lx when 0 then '正常' when 1 then '样机' when 2 then '赠品' when 3 then '残次' end as lxInfo
            from mf_fc
        <where>
            <if test="lsh != null and lsh!= ''">
                lsh=#{lsh}
            </if>
        </where>
            order by lrsj desc
    </select>
    <select id="selectUserByPage" resultType="com.mallfe.item.pojo.User">
        select id,username,password,truename,lx,
               case lx
                   when 0 then '系统管理员'
                   when 1 then '库管员'
                   when 2 then '开单员'
                   when 3 then '审核员' end as lxInfo
        from mf_user

        order by id
    </select>
</mapper>
